/*const showData = (data) => {
  let show = '';
  for (let i=0; i<data.length; i++) {
    let call = `<li><a href = ""> ${data[i].indicatorName}</a></li>`;

    show += call
  }
  return show;
};


const information = Object.keys(menuPaises[0].data);
const porcentaje = Object.values(menuPaises[0].data);
const nuevaData = () => {
  let arreglo = [];
  for (let i=0; i<information.length; i++) {
  arreglo.push(information[i],porcentaje[i])
console.log(arreglo);
return arreglo;
}
}
nuevaData();*/